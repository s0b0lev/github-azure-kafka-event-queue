/* eslint-disable import/no-commonjs */

/* NOTE: This module is actually not used by the current setup, at least it
 * seems so. */

/**
 * Bootstrap for babel.
 */
require('@babel/register');
require('./src/server');
